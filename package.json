{
  "version": "0.1.3",
  "name": "ficon",
  "description": "",
  "main": "./lib",
  "author": "Benjamin Kniffler <bkniffler@me.com>",
  "license": "MIT",
  "scripts": {
    "up": "yarn upgrade-interactive --latest",
    "netlify": "yarn build && cd example/react && yarn && yarn build",
    "prepublishOnly": "yarn build && yarn test",
    "build:free:regular": "yarn clean && ts-node scripts/generate free regular,brands && tsc",
    "build:free:solid": "yarn clean && ts-node scripts/generate solid regular,brands && tsc",
    "build:pro:light": "yarn clean && ts-node scripts/generate pro light,brands && tsc",
    "build:pro:regular": "yarn clean && ts-node scripts/generate pro regular,brands && tsc",
    "build:pro:solid": "yarn clean && ts-node scripts/generate pro solid,brands && tsc",
    "clean": "rm-cli -r lib src/icons src/index.ts",
    "test": "jest",
    "coverage": "codecov"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.3.1",
    "@fortawesome/fontawesome-pro": "^5.3.1",
    "@types/decompress": "^4.2.3",
    "@types/fs-extra": "^5.0.4",
    "@types/jest": "^23.3.2",
    "@types/lodash": "^4.14.116",
    "@types/node": "^10.11.0",
    "@types/react": "^16.4.14",
    "@types/react-dom": "^16.0.7",
    "@types/react-test-renderer": "^16.0.2",
    "aws-sdk": "^2.323.0",
    "codecov": "^3.1.0",
    "fs-extra": "^7.0.0",
    "glob-promise": "^3.4.0",
    "jest": "^23.6.0",
    "react": "^16.5.2",
    "react-fela": "^8.0.2",
    "react-test-renderer": "^16.5.2",
    "rm-cli": "^1.4.2",
    "tar": "^4.4.6",
    "ts-jest": "^23.10.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  },
  "peerDependencies": {
    "react": "^16.5.2",
    "react-fela": "^8.0.0"
  },
  "dependencies": {},
  "jest": {
    "preset": "ts-jest",
    "collectCoverage": true,
    "collectCoverageFrom": [
      "src/**/*.ts"
    ],
    "coverageReporters": [
      "html",
      "cobertura",
      "text"
    ]
  }
}
